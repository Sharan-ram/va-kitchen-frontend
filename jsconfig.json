{
  "compilerOptions": {
    "skipLibCheck": true, // Skip type checking for libraries in node_modules
    "checkJs": false, // Disable type checking for JS files
    "noEmit": true, // Ensure no files are emitted
    "types": [] // Ensure no extra @types packages are loaded
  },
  "exclude": [
    "node_modules/@types/whatwg-url" // Exclude the specific problematic package
  ]
}
